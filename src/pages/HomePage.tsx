import FormsList from "./FormsList"

function HomePage() {
    return <div>
        <h1 className="text-center h-96 flex items-center justify-center">Welcome to Form Builder App</h1>
        <FormsList />
    </div>
}

export default HomePage